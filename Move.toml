[package]
name = "mx-bridge-sc-sui"
edition = "2024.beta"

[dependencies]
locked_token = { git = "https://github.com/multiversx/mx-locked-token-sc-sui.git", rev = "dependency-fix", subdir = "" }

[addresses]
shared_structs = "0x0"
safe_module = "0x0"
bridge_safe = "0x0"
utils = "0x0"
